# Longform Scale Phase 34: 章节生成注入长篇记忆

## 目标

章节生成应直接获得长篇记忆上下文，包括全书、当前卷、当前剧情弧和近期章节摘要。否则千章项目只依赖 Setup、上一章片段和局部检索，连续性不足。

## 验收标准

1. 章节生成提示词包含长篇上下文块。
2. 长篇上下文只包含目标章节之前或截至目标章节的安全记忆，不泄漏未来章节。
3. 长篇上下文进入 trace context blocks，便于回溯生成依据。
4. 相关章节生成测试、后端全量测试和前端验证通过。
