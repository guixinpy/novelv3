# Longform Scale Phase 31: 长篇上下文记忆汇总聚合化

## 目标

构建章节长篇上下文时，`_safe_range_context_items` 当前会加载范围内所有 chapter memory ORM 行，再在 Python 中统计字数和近期标题。千章项目中，这会让每次上下文构建都跟章节数线性绑定。

## 验收标准

1. 上下文包仍返回全书、当前卷、当前剧情弧、近期章节记忆。
2. 范围汇总使用数据库聚合统计和有限标题查询，不再无界加载完整 `LongformMemory` 行。
3. 长篇规模测试和全量验证通过。
