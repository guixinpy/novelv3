# Longform Scale Phase 44: 全文导出流式化

## 目标

百万字、千章级项目导出全文时，后端不应把所有章节正文一次性加载到 Python 列表中。本阶段保持导出接口和内容格式不变，将章节正文输出改为分批迭代，降低峰值内存。

## 验收标准

1. Markdown/TXT 导出仍返回原有下载响应和内容。
2. 章节正文查询不再通过 `.all()` 一次性加载全部章节。
3. 显式章节范围仍生效。
4. 导出测试、后端测试、前端测试、类型检查和构建通过。
