# Longform Scale Phase 36: 中文字数统计去重

## 目标

当前字数统计会把整段中文额外算作一个英文词，导致每个非空中文段落轻微虚高。百万字项目需要更稳定的字数统计：英文按词计数，中文按 CJK 字符计数，混排时不重复计算中文片段。

## 验收标准

1. 纯中文文本只按 CJK 字符计数。
2. 中英混排时英文单词和 CJK 字符分别计数，不重复计算中文片段。
3. 章节生成和章节版本应用共用同一字数统计函数。
4. 相关测试和完整验证通过。
