# Longform Scale Phase 32: 世界事实台账分页加载

## 目标

长篇项目会产生远超 500 条世界事实。当前前端 truth ledger 只请求固定 500 条事实声明，超过部分会静默缺失。本阶段改为首屏分页加载，并提供继续加载入口，避免一次性拉取和渲染全部事实。

## 验收标准

1. `loadFactClaims()` 只加载第一页事实声明。
2. `loadMoreFactClaims()` 使用 offset 追加下一页。
3. `TruthLedger` 在还有更多事实时显示加载入口并触发事件。
4. 现有世界模型前端测试、类型检查和构建通过。
