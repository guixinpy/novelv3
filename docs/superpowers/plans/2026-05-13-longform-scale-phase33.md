# Longform Scale Phase 33: 检索索引增量重建

## 目标

千章项目执行检索重建时，不应删除并重嵌入所有未变化章节。全量 reindex 应保留内容哈希、来源、embedding 状态未变化的文档，只重建新增、变更或 embedding 缺失的来源，并清理已不存在的来源。

## 验收标准

1. 第二次重建未变化项目时，保留已有检索文档，不重新生成 chunks/embeddings。
2. 单章正文变化后，只重建该章节对应检索文档。
3. 清理逻辑仍避免逐文档加载 chunk id。
4. 相关后端检索测试通过，提交前跑完整验证。
